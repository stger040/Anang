# Copy to .env.neon (gitignored) and paste the same DATABASE_URL you use in Vercel.
# Then from repo root: npm run db:push:neon -w @anang/platform-app
#                          npm run db:seed:neon -w @anang/platform-app
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"
